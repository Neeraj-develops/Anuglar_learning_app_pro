import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Android-learning.My-first-app',
  appName: 'Angular_app_pro',
  webDir: 'www'
};

export default config;
